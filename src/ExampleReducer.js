// const reducerFunction = (previousState, instructions) => {
    //-- return the NEW state to override the previousState
        //-- switch structure, depending on the instruction we receiver,
        //-- the state will update in one way or another
    // return newState

// }